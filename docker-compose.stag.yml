version: '2.1'
services:
  db:
    restart: always
  worker:
    restart: always
  server:
    restart: always
    env_file:
      - stag_secrets.env
    environment:
      - ENV=stag
      - PYTHONUNBUFFERED=1
    command:
      "main-server"
