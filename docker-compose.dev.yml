version: '2'
services:
  db:
    restart: unless-stopped
    env_file:
      - dev_secrets.env
  server:
    restart: unless-stopped
    env_file:
      - dev_secrets.env
    environment:
      - ENV=dev
      - PYTHONUNBUFFERED=1
      - FLASK_ENV=development
    volumes:
      - ./server/:/app
      - $HOME/.aws/credentials:/root/.aws/credentials:ro
    command:
      "dev"
