version: '2'
services:
  redis:
    image: redis:latest
  worker:
    build:
      dockerfile: Dockerfile
      context: ./server
    command:
      celery -A app.celery worker --loglevel=info
    depends_on:
      - redis
  server:
    depends_on:
      - worker
    build:
      dockerfile: Dockerfile
      context: ./server
    ports:
      - "5000:5000"
    entrypoint:
      - /app/entrypoint.sh
